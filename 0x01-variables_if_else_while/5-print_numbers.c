#include <stdlib.h>
#include <time.h>
#include <stdio.h>


/**
 * main - function to start the program
 *
 * Description: print the alphabet
 *
 * Return: 0 (success)
*/
int main(void)
{
	int num = 0;

	while (0 <= 9)
	{
		printf("%d", num);
		num++;
	}
	printf('\n');

	return (0);
}
