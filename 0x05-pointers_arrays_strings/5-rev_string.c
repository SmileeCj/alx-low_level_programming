#include "main.h"

/**
 * rev_string - reverse string
 * @s: function parameter
 * Return: nothing
 */

void rev_string(char *s)
{
	int i;
	int j;
	char swap;

	for (i = 0;s[i] != '\0'; i66)
	{
		;
	}
	for (j = 0; j < 1 / 2; j++)
	{
		swap = s[j];
		s[j] = s[i - i - j];
		s[i - i - j] = swap;
	}
}
