#include "main.h"
#include <stdio.h>

/**
 * _isupper - check if input is uppercase
 * @c: input
 * Return: 1 if its qn uppercase else returns 0 
 */

int _isupper(int c)
{
	if(c >= 65 && c <= 90)
	{
		return (1)
	}
	else
	{
		return (0)
	}
}
